import * as React from 'react';
import { App } from '../pages/App';

const IndexPage = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default IndexPage;
